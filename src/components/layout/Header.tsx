import { useState } from 'react'
import { Button } from '../ui/Button'
import { Logo } from './Logo'
type HeaderProps = { onStartTrip: () => void; onDashboard: () => void; isDashboard: boolean }
export function Header({ onStartTrip, onDashboard, isDashboard }: HeaderProps) { const [menuOpen, setMenuOpen] = useState(false); const goDashboard = () => { onDashboard(); setMenuOpen(false) }; const startTrip = () => { onStartTrip(); setMenuOpen(false) }; return <header className="site-header"><div className="header-inner"><Logo onDashboard={goDashboard} /><button className="menu-toggle" type="button" aria-expanded={menuOpen} aria-controls="primary-navigation" onClick={() => setMenuOpen((open) => !open)}><span className="sr-only">Toggle navigation</span><span /> <span /></button><div className={`header-navigation ${menuOpen ? 'is-open' : ''}`} id="primary-navigation"><nav aria-label="Primary navigation"><button className={`nav-link ${isDashboard ? 'is-active' : ''}`} onClick={goDashboard} aria-current={isDashboard ? 'page' : undefined}>Dashboard</button><button className={`nav-link ${!isDashboard ? 'is-active' : ''}`} onClick={startTrip} aria-current={!isDashboard ? 'page' : undefined}>Trips</button></nav><div className="header-actions"><Button className="header-trip-button" onClick={startTrip} icon="plus">New trip</Button><button className="profile-button" type="button" aria-label="Open profile menu"><span aria-hidden="true">TC</span></button></div></div></div></header> }
