export interface DestinationSuggestion{city:string;country:string;label:string} export const locationsApi={async search(q:string):Promise<DestinationSuggestion[]>{const all=['Goa, India','Manali, India','Jaipur, India','Bali, Indonesia','Bangkok, Thailand'].map(label=>{const[city,country]=label.split(', ');return{city,country,label}});return all.filter(x=>x.label.toLowerCase().includes(q.toLowerCase())).slice(0,5)}};
