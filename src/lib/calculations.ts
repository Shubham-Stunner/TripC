import {Expense, ExpenseCategory, Member, Settlement, TripSummary} from '../types';
export const categories:ExpenseCategory[]=['Travelling','Tickets','Food','Activities','Hotel','Shopping','Other'];
export function calculateSummary(tripId:string,members:Member[],expenses:Expense[]):TripSummary{const totalSpent=expenses.reduce((s,e)=>s+e.amount,0);const categoryTotals=Object.fromEntries(categories.map(c=>[c,0])) as Record<ExpenseCategory,number>;expenses.forEach(e=>categoryTotals[e.category]+=e.amount);const perPerson=members.length?totalSpent/members.length:0;const memberBalances=members.map(m=>{const paid=expenses.filter(e=>e.paidBy===m.id).reduce((s,e)=>s+e.amount,0);return{memberId:m.id,paid,share:perPerson,balance:paid-perPerson}});const debtors=memberBalances.filter(b=>b.balance<-.01).map(b=>({...b,balance:-b.balance}));const creditors=memberBalances.filter(b=>b.balance>.01).map(b=>({...b}));const settlements:Settlement[]=[];let i=0,j=0;while(i<debtors.length&&j<creditors.length){const amount=Math.min(debtors[i].balance,creditors[j].balance);settlements.push({fromMemberId:debtors[i].memberId,toMemberId:creditors[j].memberId,amount});debtors[i].balance-=amount;creditors[j].balance-=amount;if(debtors[i].balance<.01)i++;if(creditors[j].balance<.01)j++;}return{tripId,totalSpent,perPerson,expenseCount:expenses.length,unsettledTotal:settlements.reduce((s,x)=>s+x.amount,0),categoryTotals,memberBalances,settlements};}
